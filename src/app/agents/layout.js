export const metadata = {
  title: 'Стань агентом — зарабатывай 2% с каждого заказа',
  description: 'Подключай магазины к платформе и получай 2% от каждого заказа. Без вложений. Оформляй заказы через приложение вместо бумажки.',
};

export default function Layout({ children }) {
  return children;
}
