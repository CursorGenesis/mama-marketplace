export const metadata = {
  title: 'Карта поставщиков Кыргызстана — MarketKG',
  description: 'Интерактивная карта поставщиков продуктов питания. Бишкек, Ош, Джалал-Абад, Каракол и все регионы Кыргызстана.',
};

export default function Layout({ children }) {
  return children;
}
