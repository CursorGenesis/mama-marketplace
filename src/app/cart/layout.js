export const metadata = {
  title: 'Корзина — MarketKG',
  description: 'Оформите заказ на MarketKG. Товары разделены по поставщикам, каждому отправляется отдельная заявка.',
};

export default function Layout({ children }) {
  return children;
}
