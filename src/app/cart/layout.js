export const metadata = {
  title: 'Корзина — Arzaman.kg',
  description: 'Оформите заказ на Arzaman.kg. Товары разделены по поставщикам, каждому отправляется отдельная заявка.',
};

export default function Layout({ children }) {
  return children;
}
